_inter:
  mov %rbx, 10
  mov %rcx, 5
  mov %rdx, 4
  mov %rbx, 4
  mov %rdi, %rbx
  add %rdi, %rcx
  mov %rsi, %rdi
  mov %rbx, 5
  mov %r8, %rsi
  add %r8, %rdx
  mov %r9, %r8
  add %r9, %rbx
  mov %r10, %r9
  add %r10, %rcx
  mov %rax, %r10
  ret

